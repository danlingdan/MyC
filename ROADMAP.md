# C 编译器开发进度与路线图

## 1. 目前已完成的工作
目前已经成功搭建了编译器的总体骨干，实现了一个**计算器级别**的微型编译器。
具体包括：
*   **词法分析器 (Tokenizer)**：能识别整数（TK_NUM）、标点符号和运算符（TK_RESERVED），能忽略空格，支持错误定位（rror_at）。
*   **语法分析器 (Parser)**：实现了基于递归下降算法的解析器，正确处理了加减乘除和括号的结合性与优先级，并生成了抽象语法树 (AST)。
*   **代码生成器 (Code Generator)**：实现了一个基于栈机的 x86-64 汇编生成器，能将局部运算转化为汇编指令（push, pop, dd, sub, imul, idiv 等）。
*   **测试框架**：配有外部测试脚本，可以验证编译后程序的退出状态码（Exit Code）。

---

## 2. 整体大计划（高层规划）
将当前微型编译器扩展为真正的 C 语言编译器，采用**增量开发**的策略，分阶段逐步实现核心功能：
*   **阶段一：运算扩展**（完善表达式级语义）
*   **阶段二：变量与多行语句**（引入内存布局与赋值）
*   **阶段三：控制流**（实现分支与循环控制）
*   **阶段四：指针与数组**（C 语言的核心灵魂）
*   **阶段五：函数与调用约定**（模块化执行与栈帧管理）
*   **阶段六：类型系统与结构体**（支持非 int 类型、聚合数据和预处理）

---

## 3. 具体细分计划与待办事项

### 阶段一：运算扩展
- [x] **支持关系运算符**：实现 ==, !=, <, <=, >, >=。需要在词法分析中支持多字符操作符，并在后端的汇编中引入 cmp, sete, setl 等指令。
- [x] **支持一元操作符**：实现正号 + 和负号 - （如 x = -5）。

### 阶段二：变量与多行语句
- [ ] **支持多行语句与分号**：引入语句（Expression Statement），支持以 ; 结尾的代码，顺序执行。
- [ ] **单字母局部变量**：支持  到 z 的变量。要求利用基址寄存器（
bp）加上偏移量来读写栈区内存。
- [ ] **多字母变量名与赋值**：扩展词法分析以识别关键字/标识符，实现赋值操作符 =（需梳理左值 lvalue 和右值 rvalue 的概念）。

### 阶段三：控制流
- [ ] **引入 
eturn 语句**：支持通过明确的 
eturn 语句来返回结果并结束函数。
- [ ] **if 与 if-else 分支**：在 AST 中引入控制节点，在汇编生成中配合标签（Labels）和跳转指令（je, jmp 等）。
- [ ] **循环语句**：支持 while 和 or 循环。

### 阶段四：指针与数组
- [ ] **一元取址和解引用**：引入 &（取地址）和 *（解引用指针）。
- [ ] **指针类型的算术运算**：支持指针加减法（如 p + 1，增加的量基于具体类型的 sizeof）。
- [ ] **一维与多维数组**：将数组名降级为指向其首元素的指针特性。

### 阶段五：函数与全局机制
- [ ] **代码块解析**：支持 { ... } 组成的复合语句块。
- [ ] **无参函数调用与定义**：调用其他函数的汇编生成，以及全局函数的声明实现。
- [ ] **带参函数与调用约定**：适配 System V ABI 调用约定，利用寄存器 
di, 
si, 
dx, 
cx, 
8, 
9 传递前6个参数。

### 阶段六：类型系统与进阶结构
- [ ] **基础类型隔离**：引入 int 和 char 关键字，根据不同字节长度进行数据存取（如 movsx, movsb）。
- [ ] **字符串字面量**：支持处理 "hello" 并在汇编端把它放入 .data 或 .rodata 段。
- [ ] **结构体（struct）进阶**：支持内存的聚合排列，以及成员存取的 . 和 ->。
